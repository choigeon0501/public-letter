# 손편지 서비스 MVP 기획안

> 작성일: 2026-09-21  
> 버전: v0.1 (Flow 검증용 MVP)

---

## 0. 작업 시작 전 필수 지시 (AI 개발자용)

**개발을 시작하기 전에 아래 사이트를 먼저 직접 방문해서 UI/UX와 흐름을 파악할 것.** 이 문서의 화면 설계는 이 서비스들을 참고해서 작성되었으며, 톤앤매너·편지지 카탈로그 구성·작성 화면·완료 화면의 느낌을 실제로 보고 나서 구현에 들어간다.

| 사이트 | URL | 주로 볼 것 |
|---|---|---|
| 온기우편함 | https://www.xn--vo5b23idue.com/ | 편지 서비스의 감성적 톤, 랜딩 구성 |
| LetterTo | https://letterto.swygbro.com/complete | 완료 페이지에서 편지를 보여주는 방식 (JS 필요) |
| 편지쓰다 | https://xn--9b6b95p.kr/ | 편지 작성 UI |
| 온기박스 | https://ongibox.co.kr/ | 편지 상품 소개/선택 구성 |
| 이지온메일 | https://easyonmail.com/main | **가장 중요.** 편지지 선택 → 작성 → 발송정보 → 접수 4단계 구조, 편지지 카탈로그 그리드, 글꼴 선택, 미리보기 |

확인 순서 권장: 이지온메일 → 편지쓰다 → LetterTo → 나머지.  
사이트 확인이 끝난 뒤 이 문서의 1~7장을 읽고, 7장 "개발 순서 제안"대로 진행한다. 불명확한 부분은 6장 "확인이 필요한 사항"을 참고하고, 거기에 없는 사항은 임의로 결정하지 말고 질문할 것.

---

## 1. 개요

### 1.1 한 줄 정의
사용자가 웹에서 편지지를 고르고 편지를 쓰면, **실제 손글씨로 써서 우편 발송**해주는 서비스.

### 1.2 이번 MVP의 목표
- 하드웨어(손글씨 로봇) 연동, 결제, 로그인, DB는 **모두 제외**.
- "편지지 선택 → 편지 작성(AI 초안 포함) → 주소 입력 → 완료/확인" **UX Flow를 실제로 동작하는 화면으로 검증**하는 것이 목표.
- 완료 시점의 데이터(편지지 ID, 본문, 수신/발신 정보)는 이후 손글씨 출력 파이프라인의 입력 포맷이 되므로, 구조만 정의해 둔다.

### 1.3 참고 서비스
| 서비스 | 참고 포인트 |
|---|---|
| 이지온메일 (easyonmail.com) | 편지지 선택 → 작성 → 발송정보 → 접수의 4단계 구조, 편지지 카탈로그 UI, 글꼴 선택 |
| LetterTo (letterto.swygbro.com) | 완료 페이지에서 편지 결과물을 보여주는 방식 |
| 온기우편함, 편지쓰다 등 | 편지 작성 UI 톤앤매너 |

---

## 2. 전체 사용자 흐름

```
[1] 랜딩/편지지 선택  →  [2] 편지 작성 (+AI 도움)  →  [3] 발송 정보 입력  →  [4] 완료 & 편지 확인
     /                        /write                      /address                  /complete
```

- 로그인 없음. 새로고침 시 데이터가 날아가도 무방하지만, 편의상 `sessionStorage`에 작성 중 상태를 저장한다.
- 각 단계 상단에 스텝 인디케이터(1/4 ~ 4/4)를 노출한다.
- 이전 단계로 돌아가도 입력값은 유지된다.

---

## 3. 화면별 상세

### 3.1 [1] 편지지 선택 페이지 (`/`)

**목적:** 사용자가 편지지를 고르고, 실제 작성 화면이 어떻게 보일지 미리 확인한다.

**구성**
- 서비스 소개 헤더(짧은 카피 1~2줄 + "편지 쓰기 시작" 버튼은 없이 바로 카탈로그 노출).
- 편지지 카탈로그: 8종 (최대 10종). 그리드 형태, 카드 클릭 시 선택.
- 우측(모바일은 하단 시트)에 **미리보기 패널**: 선택된 편지지 위에 샘플 텍스트("안녕, 잘 지내지? …")가 올라간 상태로 렌더링. 실제 작성 화면과 동일한 컴포넌트를 재사용한다.
- 하단 CTA: **"이 편지지로 쓰기"** → `/write?paper={id}`

**편지지 8종 (초안)**
| ID | 이름 | 컨셉 | 비고 |
|---|---|---|---|
| `plain-cream` | 크림 무지 | 담백한 기본형, 줄 없음 | 기본 선택 |
| `lined-white` | 화이트 줄노트 | 줄이 있는 클래식 편지지 | 손글씨 출력에 가장 유리 |
| `kraft` | 크라프트 | 갈색 종이 질감, 심플 | |
| `floral` | 들꽃 | 상단/하단 은은한 꽃 일러스트 | 가족/감사 |
| `night-sky` | 밤하늘 | 어두운 남색 테두리 + 별 | 연인/그리움 |
| `seaside` | 바닷가 | 파란 계열 수채 느낌 | 안부 |
| `season-autumn` | 가을 | 낙엽/따뜻한 톤 | 시즌 |
| `celebration` | 축하 | 컨페티, 밝은 톤 | 생일/축하 |

- 모든 편지지는 **CSS + SVG로 구현**(이미지 파일 최소화). 손글씨 출력 시 배경과 텍스트 영역이 분리되어야 하므로, 각 편지지는 `배경 레이어`와 `텍스트 영역(safe area)` 좌표를 메타데이터로 갖는다.
- 편지지 메타데이터 예시:
  ```ts
  type LetterPaper = {
    id: string;
    name: string;
    description: string;
    background: string;      // CSS class 또는 SVG 경로
    textArea: { top: number; left: number; width: number; height: number }; // %
    lineHeight: number;      // 줄 간격(px)
    maxChars: number;        // 편지지 1장 기준 글자 수 상한
    defaultFont: string;
  }
  ```

---

### 3.2 [2] 편지 작성 페이지 (`/write`)

**목적:** 선택한 편지지 위에서 직접 편지를 쓴다. 막힐 때 AI 초안 도움을 받을 수 있다.

**구성**
- 선택된 편지지가 화면 중앙에 실제 비율로 표시된다.
- 편지지 안에 세 개의 입력 영역:
  1. **To (받는 사람 호칭)** — 상단, 예: "사랑하는 할머니께"
  2. **본문** — 편지지의 텍스트 영역에 맞춘 `textarea` (배경 투명, 편지지 줄 간격에 맞춤). 클릭하면 바로 타이핑 가능.
  3. **From (보내는 사람)** — 하단 우측, 예: "2026년 9월, 손주 민수 올림"
- 글꼴 선택(손글씨 계열 3종 정도, 예: 나눔손글씨 계열 무료 폰트). 실제 손글씨 출력에서는 무시될 수 있으나, 작성 몰입감을 위해 제공.
- 글자 수 카운터: `현재 글자 수 / maxChars`. 초과 시 경고(편지지 1장 기준).
- **AI 도움 버튼**: 본문 영역 우상단(또는 툴바)에 "✨ AI에게 초안 부탁하기" 버튼. 본문이 비어 있으면 강조 표시, 내용이 있으면 "다듬기" 로도 사용 가능(v0.1은 초안 생성만).
- 하단 CTA: **"작성 완료"** → `/address`. To/본문이 비어 있으면 비활성.
- 좌하단 "편지지 다시 고르기" (본문 유지한 채 `/`로 이동).

**AI 초안 도움 모달**
- 버튼 클릭 시 모달(모바일은 바텀시트) 오픈. 입력 3개만 받는다.

| 순서 | 질문 | 입력 형태 | 예시 |
|---|---|---|---|
| 1 | 누구에게 쓰는 편지인가요? | 짧은 텍스트 | "할머니", "군대 간 남자친구" |
| 2 | 나는 그 사람에게 어떤 존재인가요? | 칩 선택 + 직접 입력 (친구 / 연인 / 가족 / 손주·자녀 / 부모 / 동료 / 기타) | "손주" |
| 3 | 어떤 내용을 담고 싶나요? | 여러 줄 텍스트 (2~3문장이면 충분) | "추석에 못 가서 죄송하고, 건강 챙기시라는 말" |

- "초안 만들기" 클릭 → 로딩(스트리밍 표시) → 결과를 모달 안에서 미리 보여줌.
- 결과 하단 액션: **"편지지에 넣기"**(본문에 삽입, 기존 텍스트 있으면 덮어쓸지 확인) / **"다시 생성"** / **"닫기"**.
- 생성 길이는 편지지의 `maxChars`를 넘지 않도록 프롬프트에 제한을 넣는다.

**AI 프롬프트 설계 (요지)**
- System: "너는 한국어 손편지 작가다. 과장하지 않고 담백하게, 실제 사람이 손으로 쓸 법한 문장으로 쓴다. 인사 → 본론 → 마무리 구조. 이모지·마크다운 금지. {maxChars}자 이내. 호칭과 존댓말/반말은 관계에 맞게 자동 결정."
- User: 받는 사람 / 관계 / 담고 싶은 내용 3가지를 그대로 전달.
- 출력은 본문 텍스트만(To/From은 사용자가 이미 입력한 값 유지).

---

### 3.3 [3] 발송 정보 입력 페이지 (`/address`)

**목적:** 실제 우편 발송에 필요한 수신자·발신자 정보를 받는다.

**받는 사람 (수신자)**
| 항목 | 필수 | 비고 |
|---|---|---|
| 이름 | O | |
| 우편번호 | O | Daum(Kakao) 우편번호 서비스로 자동 입력 |
| 기본 주소 | O | 우편번호 검색으로 자동 입력, 수정 불가 |
| 상세 주소 | O | 직접 입력 (동/호수 등) |
| 전화번호 | △ | 선택. 등기·배송 문의용. MVP에서는 **선택 입력**으로 두고 안내 문구 표시 |

**보내는 사람 (발신자)**
| 항목 | 필수 | 비고 |
|---|---|---|
| 이름 | O | 작성 페이지 From 값이 있으면 기본값으로 채움 |
| 우편번호 / 기본 주소 / 상세 주소 | O | 동일하게 Daum 우편번호 서비스 사용 |
| 전화번호 | △ | 선택. 반송/문의 연락처 |

- **"받는 사람과 동일"** 체크박스는 불필요(의미 없음). 대신 "내 주소 기억하기"는 로그인이 없으므로 제외.
- Daum 우편번호 서비스는 `https://t1.daumcdn.net/mapjsapi/bundle/postcode/prod/postcode.v2.js`를 스크립트로 로드하고, 팝업/임베드 방식 중 **임베드(레이어) 방식** 사용(모바일 UX 고려).
- 하단 CTA: **"편지 보내기"** → 유효성 검사 통과 시 `/complete`. 실패 시 필드별 에러 표시.
- 좌하단 "이전"(작성 페이지로, 본문 유지).

---

### 3.4 [4] 완료 & 편지 확인 페이지 (`/complete`)

**목적:** 접수가 완료되었음을 알리고, 사용자가 보낸 편지를 다시 보여준다.

**구성**
- 상단 완료 메시지: "편지가 접수되었어요. 정성껏 손으로 써서 보내드릴게요." + 예상 발송 안내(고정 문구, 예: "영업일 기준 2~3일 내 발송").
- **봉투 → 편지 열림 애니메이션**(선택 사항, 간단한 CSS 트랜지션): 봉투 겉면에 받는 사람 이름/주소 요약이 보이고, 클릭하면 편지지 전체가 펼쳐진다.
- 편지 본문: 작성 페이지와 동일한 편지지 컴포넌트를 **읽기 전용**으로 렌더링(To/본문/From/글꼴 그대로).
- 발송 정보 요약 카드: 받는 사람 이름·주소, 보내는 사람 이름·주소.
- 액션 버튼:
  - "이미지로 저장" (html-to-image 등으로 편지지 PNG 저장 — 선택)
  - "새 편지 쓰기" → 상태 초기화 후 `/`
- MVP에서는 서버 저장 없이 클라이언트 상태만으로 렌더. 새로고침 시 상태가 없으면 `/`로 리다이렉트.

---

## 4. 기술 스택 & 구조

### 4.1 스택
- **Next.js 15 (App Router) + TypeScript**
- 스타일: Tailwind CSS (편지지 배경은 CSS/SVG)
- 상태 관리: React Context 또는 Zustand + `sessionStorage` 동기화
- AI: **Anthropic Claude API** (`@anthropic-ai/sdk`), 서버 Route Handler에서 호출, 클라이언트는 키를 갖지 않음
- 주소: Daum 우편번호 서비스 (`react-daum-postcode` 사용 가능)
- 폰트: 무료 손글씨 웹폰트 3종 (`next/font/local` 또는 CDN)
- 배포: Vercel

### 4.2 폴더 구조 (초안)
```
app/
  layout.tsx
  page.tsx                  # [1] 편지지 선택
  write/page.tsx            # [2] 편지 작성
  address/page.tsx          # [3] 발송 정보
  complete/page.tsx         # [4] 완료
  api/
    ai/draft/route.ts       # Claude 초안 생성 (POST, 스트리밍)
components/
  StepIndicator.tsx
  paper/
    LetterPaper.tsx         # 편지지 렌더러 (편집/읽기 모드 공용)
    PaperCard.tsx           # 카탈로그 카드
    papers.ts               # 편지지 8종 메타데이터
  write/
    LetterEditor.tsx
    AiDraftModal.tsx
    FontPicker.tsx
  address/
    AddressForm.tsx
    PostcodeSearch.tsx
  complete/
    EnvelopeReveal.tsx
lib/
  store.ts                  # 편지 상태 (Zustand)
  claude.ts                 # Anthropic 클라이언트
  validation.ts             # 주소 폼 유효성 (zod)
```

### 4.3 편지 상태 모델 (DB 없음, 클라이언트 보관)
```ts
type LetterState = {
  paperId: string;
  font: string;
  to: string;         // 호칭
  body: string;
  from: string;
  recipient: Address;
  sender: Address;
};

type Address = {
  name: string;
  zonecode: string;   // 우편번호
  address: string;    // 기본 주소
  detail: string;     // 상세 주소
  phone?: string;
};
```
> 이 구조가 이후 손글씨 출력 파이프라인(`/api/orders` 등)의 요청 본문이 된다.

### 4.4 AI API 스펙
- `POST /api/ai/draft`
- Request
  ```json
  {
    "recipient": "할머니",
    "relationship": "손주",
    "intent": "추석에 못 가서 죄송하고 건강 챙기시라는 말",
    "maxChars": 600
  }
  ```
- Response: `text/event-stream` (스트리밍) 또는 단순 JSON `{ "draft": "..." }`. MVP는 스트리밍 권장(체감 속도).
- 서버에서 `ANTHROPIC_API_KEY` 환경변수 사용. 모델은 비용/속도 균형으로 Sonnet 계열 사용, 환경변수로 교체 가능하게.
- 입력 길이 제한(각 필드 200자), 간단한 레이트리밋(IP 기준 분당 N회)은 MVP에서도 넣어둔다(키 노출 없이 남용 방지).

---

## 5. 범위

### 5.1 이번 MVP에 포함
- 4단계 Flow 전체 화면
- 편지지 8종 + 미리보기
- 편지지 위 직접 작성 + 글꼴 선택 + 글자 수 제한
- AI 초안 생성(3문항 → Claude)
- Daum 우편번호 연동 주소 입력 + 유효성 검사
- 완료 페이지에서 편지 결과물 확인
- 모바일/데스크톱 반응형

### 5.2 제외 (다음 단계)
- 로그인/회원, 주소록, 결제
- 서버 저장(DB), 주문 이력, 관리자 화면
- 손글씨 로봇/출력 하드웨어 연동 (완료 시점 데이터만 정의)
- 사진 동봉, 봉투 디자인 선택, 우편 종류(등기 등) 선택
- AI "다듬기/이어쓰기" 기능 (v0.2 후보)
- 편지지 2장 이상 넘어가는 장문 처리

---

## 6. 확인이 필요한 사항

1. **전화번호**: 수신자/발신자 모두 선택 입력으로 두었는데, 실제 발송 시 필수인지(등기 여부 등) 확인 필요. 필수라면 발신자만 필수로 두는 것도 방법.
2. **편지지 글자 수 상한**: 손글씨 출력 시 편지지 1장에 몇 자까지 들어가는지에 따라 `maxChars`가 결정됨. 현재 임시값 500~700자.
3. **To/From 위치**: 편지지마다 위치가 다를 수 있는데, 편지지 메타데이터에 포함시킬지, 모든 편지지에서 동일 위치로 고정할지.
4. **AI 초안의 존댓말/반말**: 관계로 자동 판단하게 할지, 사용자가 토글로 선택하게 할지.
5. **완료 페이지 저장 기능**: "이미지로 저장"을 MVP에 넣을지(공유 유도용으로는 효과 있음).
6. **랜딩 페이지 분리 여부**: 현재는 `/`가 바로 편지지 선택. 서비스 소개용 랜딩이 따로 필요한지.

---

## 7. 개발 순서 제안

1. 편지지 8종 메타데이터 + `LetterPaper` 렌더러 (편집/읽기 공용) — 전체 화면의 핵심 컴포넌트
2. 편지지 선택 페이지 + 미리보기
3. 작성 페이지(에디터, 글꼴, 글자 수)
4. AI 초안 모달 + `/api/ai/draft` Route Handler
5. 주소 페이지 + Daum 우편번호 + 유효성
6. 완료 페이지 + 상태 초기화
7. 반응형 다듬기, sessionStorage 복원, 빈 상태 리다이렉트
